import 'dart:ui';

import 'package:flutter/material.dart';

class BeerColors {
  static const grey = Color.fromRGBO(54, 54, 54, 0.05);
  static const unselected = Color.fromRGBO(54, 54, 54, 0.5);
  static const black = Color.fromRGBO(54, 54, 54, 1);
  static const white = Colors.white;
  static const errorBackGround = Color.fromRGBO(255, 0, 0, 0.5);
}
